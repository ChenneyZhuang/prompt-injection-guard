# DeepSeek API key (optional — if unset, falls back to regex pattern matching)
DEEPSEEK_API_KEY=sk-your-key-here

# DeepSeek API base URL (optional, defaults to DeepSeek official endpoint)
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
